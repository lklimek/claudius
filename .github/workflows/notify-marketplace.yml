name: Notify Marketplace

on:
  push:
    branches: [main]
    paths:
      - '.claude-plugin/plugin.json'
  workflow_dispatch:

# The job authenticates with AGENTES_PAT (below), never the default GITHUB_TOKEN,
# so no default-token scopes are needed.
permissions: {}

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger marketplace workflow
        run: |
          gh workflow run --repo lklimek/agents update.yml
        env:
          # AGENTES_PAT must have either the `actions:write` scope (recommended) or the `repo` scope
          # so that `gh workflow run` can trigger workflows in the lklimek/agents repository.
          GH_TOKEN: ${{ secrets.AGENTES_PAT }}
