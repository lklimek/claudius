{
  "description": "Claudius plugin hooks — GitHub write access control",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__plugin_claudius_github__.*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/block-github-writes.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If persistent memory tools are available (memcan), search for project context: architecture decisions, coding conventions, known pitfalls, pending TODOs. Use `memcan:recall` skill. If memcan is unavailable, skip silently."
          }
        ]
      }
    ]
  }
}
